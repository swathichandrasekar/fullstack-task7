<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>User Profile Card</title>
  <style>
    body {
      margin: 0;
      font-family: "Segoe UI", sans-serif;
      background: linear-gradient(to right, #e8f0ff, #f5faff);
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      padding: 20px;
    }

    .card {
      background: #fff;
      border-radius: 18px;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
      padding: 40px 30px;
      width: 100%;
      max-width: 500px;
      text-align: center;
      position: relative;
    }

    .qr {
      margin-bottom: 25px;
    }

    .qr img {
      width: 110px;
      height: 110px;
      border-radius: 10px;
      border: 2px solid #007bff;
      padding: 4px;
      background: #fff;
    }

    h2 {
      font-size: 24px;
      margin-bottom: 6px;
      color: #333;
    }

    .info {
      font-size: 15.5px;
      color: #555;
      margin: 8px 0;
    }

    .section {
      margin-top: 20px;
    }

    .status-badge {
      display: inline-block;
      margin-top: 15px;
      background-color: #28a745;
      color: #fff;
      padding: 8px 16px;
      font-size: 14px;
      border-radius: 30px;
      font-weight: 600;
    }

    .btn-home {
      margin-top: 30px;
      display: inline-block;
      text-decoration: none;
      background: #007bff;
      color: white;
      padding: 12px 24px;
      border-radius: 10px;
      transition: background 0.3s ease;
    }

    .btn-home:hover {
      background: #0056b3;
    }

    .label {
      font-weight: 600;
      color: #444;
    }
  </style>
</head>
<body>
  <div class="card">
     <h2 class="card-heading">🎓 SkillUp Badge</h2>
    <div class="qr">
      <img src="<%= user.qrCode %>" alt="QR Code" />
    </div>
    <h2><%= user.name %></h2>
    <p class="info"><span class="label">User ID:</span> <%= user.id %></p>
    <p class="info"><span class="label">Email:</span> <%= user.email %></p>
    <p class="info"><span class="label">Age:</span> <%= user.age %></p>
    <p class="info"><span class="label">Course:</span> <%= user.course %></p>
    <div class="section">
      <p class="info"><span class="label">Membership Since:</span> <%= user.membershipSince %></p>
      <p class="info"><span class="label">Valid Until:</span> <%= user.membershipValidUntil %></p>
      <p class="info"><span class="label">Signup Timestamp:</span> <%= user.signupTimestamp %></p>
    </div>
    <div class="status-badge">Active</div>
    <br />
    <a href="/" class="btn-home">← Back to Home</a>
  </div>
</body>
</html>
